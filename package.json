{
  "name": "eclipse-battle-calculator",
  "version": "0.0.1",
  "packageManager": "bun@1.2.19",
  "description": "Battle calculator for Eclipse: Second Dawn for the Galaxy",
  "scripts": {
    "dev": "bun --hot src/index.html",
    "build": "bun build ./src/*.html --outdir ./dist --minify && bun build ./src/combat-worker.ts --outfile ./dist/combat-worker.js --minify && cp src/favicons/apple-touch-icon.png src/favicons/web-app-manifest-192x192.png src/favicons/web-app-manifest-512x512.png dist/ && bun run scripts/build-service-worker.ts dist",
    "test": "bun test",
    "test:engine": "bun test src/engine",
    "test:solver": "bun test src/engine/battle-rules.test.ts src/engine/battle-state.test.ts src/engine/dice-distribution.test.ts src/engine/win-probability-solver.test.ts src/engine/win-probability-solver.optimal.test.ts src/engine/win-probability-solver.minimax.test.ts src/engine/exact-combat.test.ts src/engine/optimal-damage-planner.test.ts",
    "benchmark:combat": "bun run scripts/benchmark-combat.ts",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint src scripts",
    "lint:fix": "oxlint src scripts --fix",
    "format": "oxfmt src scripts",
    "format:check": "oxfmt src scripts --check",
    "check": "bun run typecheck && bun run lint && bun run format:check && bun test && bun run build"
  },
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.0.0",
    "@types/bun": "latest",
    "oxfmt": "^0.65.0",
    "oxlint": "^1.80.0",
    "typescript": "^7.0.2"
  }
}
